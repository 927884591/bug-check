interface:
  display_name: "Bug Check"
  short_description: "Prove bug fixes from diff claims and counterexamples"
  default_prompt: "Use $bug-check as a completion proof gate: inspect the diff, state behavior claims, test them against minimal counterexamples, and verify evidence before claiming completion."
