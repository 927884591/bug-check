interface:
  display_name: "Bug Check"
  short_description: "Check changed code for bug boundaries"
  default_prompt: "Use $bug-check to check this changed scope against relevant bug boundaries and fix any missing handling."
