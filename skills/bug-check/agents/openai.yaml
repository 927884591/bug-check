interface:
  display_name: "Bug Check"
  short_description: "Check changed code with AI-selected bug boundaries"
  default_prompt: "Use $bug-check to build a context pack, select relevant bug boundaries, and fix missing handling unless this is review/audit mode."
